sudo apt update $1
sudo apt upgrade $1
sudo apt dist-upgrade $1
sudo snap refresh
sudo apt autoremove $1
sudo apt autoclean $1

#sudo apt -t xenial-backports update
#sudo apt -t xenial-backports upgrade
#sudo apt -t xenial-backports dist-upgrade
#sudo apt -t xenial-backports autoremove
#sudo apt -t xenial-backports autoclean
